# Copy to .env.local and fill in. .env.local is gitignored and must never be committed.
# Both values are safe to expose in a browser bundle — the anon/publishable key
# grants nothing on its own, because RLS denies everything without a session.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
